{
  "name": "doom",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "clean": "rimraf ./public/scripts",
    "prebuild": "yarn clean",
    "build": "tsc -p tsconfig.build.json",
    "prestart": "yarn build",
    "start": "http-server ./public",
    "test": "jest"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "http-server": "^0.12.3",
    "jest": "^26.6.3",
    "rimraf": "^3.0.2",
    "ts-jest": "^26.4.4",
    "typescript": "^4.1.3"
  }
}
